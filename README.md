# Kyiv Payload Decrypt Progress (No Images)

This site publishes brute-force progress data and map UI.

Included:
- `progress_map.html`
- `progress.json`
- `matches.tsv`

Excluded for now:
- `overlays/*.png` image assets
- `descriptors/*`

Open the live map at `progress_map.html`.
